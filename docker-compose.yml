services:
  creneau:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - creneau-data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_URL=/app/data/creneau.db
    restart: unless-stopped

volumes:
  creneau-data:
