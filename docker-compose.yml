services:
  creneau:
    image: ghcr.io/pierrewagou/creneau:latest
    ports:
      - '3000:3000'
    volumes:
      - creneau-data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/creneau.db
    restart: unless-stopped

volumes:
  creneau-data:
